# https://www.decentlab.com/products/dendrometer-for-lorawan
name: decentlab_dl_zn2
version: 2
endian: big

fields:
  - name: protocol_version
    type: u8
  - name: device_id
    type: u16
  - name: flags
    type: u16

  - flagged:
      field: flags
      groups:
        - bit: 0
          fields:
            # The vendor reads this as x[0] + x[1] * 65536: two 16-bit words,
            # low word first, so those four bytes are not a big-endian u32 and
            # cannot be read as one field. Reading only the low word also left the
            # high word unconsumed, misaligning every group after it.
            - name: dendrometer_a_position
              type: u32le16
              transform:
                - mult: 0.002384185791015625
                - add: -20000
              unit: "µm"
            - name: dendrometer_b_position
              type: u16
              unit: "µm"
              # formula: ((x[0] + x[1]*65536) / 8388608 - (x[2] + x[3]*65536) / 8388608) * 20000
        - bit: 1
          fields:
            - name: battery_voltage
              type: u16
              div: 1000
              unit: "V"
